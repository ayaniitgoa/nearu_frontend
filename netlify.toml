[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js configuration for Netlify
# The @netlify/plugin-nextjs automatically handles routing and API routes
[[plugins]]
  package = "@netlify/plugin-nextjs"

